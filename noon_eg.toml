[[dags]]
dag_id = 'farid1'
schedule_interval = '*/30 * * * *'

[[dataset]]
dag_id = 'farid1'
task_id = 'create_dataset'
datasetlist = ["fragrance"]

[[matview]]
dag_id = 'farid1'
task_id = 'create_dataset'
destination_table = `noonbimpcoma.fragrance.noonvssouq`
sql = ""
SELECT (#standardsql

select
  catalog_sku_id,
  brand,
  product_name,
  product_url,
    CASE 
  WHEN category_path in ("Hats & Caps") then "apparel"
WHEN category_path in ("Apparel, Shoes & Accessories>Clothing>Accessories>Clothing Accessories") then "apparel"
WHEN category_path in ("Skirts") then "apparel"
WHEN category_path in ("Womens Lingerie") then "apparel"
WHEN category_path in ("Apparel, Shoes & Accessories>Clothing>Underwear") then "apparel"
WHEN category_path in ("Ethnic & Traditional Wear") then "apparel"
WHEN category_path in ("Underwear") then "apparel"
WHEN category_path in ("Dresses") then "apparel"
WHEN category_path in ("Shorts") then "apparel"
WHEN category_path in ("Sleepwear") then "apparel"
WHEN category_path in ("Jackets & Coats") then "apparel"
WHEN category_path in ("Pants") then "apparel"
WHEN category_path in ("Sportswear") then "apparel"
WHEN category_path in ("Swimwear") then "apparel"
WHEN category_path in ("Tops") then "apparel"
WHEN category_path in ("Belts") then "apparel"
WHEN category_path in ("Maternity Wear") then "apparel"
WHEN category_path in ("Uniform") then "apparel"
WHEN category_path in ("Scarves & Wraps") then "apparel"
WHEN category_path in ("Suits") then "apparel"
WHEN category_path in ("Jumpsuits, Rompers & Overalls") then "apparel"
WHEN category_path in ("Accessories") then "apparel"
WHEN category_path in ("TV Mounts") then "audio_video"
WHEN category_path in ("DVD and Blu-ray Players") then "audio_video"
WHEN category_path in ("Satellite Receivers") then "audio_video"
WHEN category_path in ("CD Players & Recorders") then "audio_video"
WHEN category_path in ("Video Tapes") then "audio_video"
WHEN category_path in ("MP3 & MP4 players") then "audio_video"
WHEN category_path in ("Portable Radios") then "audio_video"
WHEN category_path in ("Home Video Accessories") then "audio_video"
WHEN category_path in ("Home Theater Systems") then "audio_video"
WHEN category_path in ("Portable Cassette & CD Players") then "audio_video"
WHEN category_path in ("Televisions") then "audio_video"
WHEN category_path in ("Microphones") then "audio_video"
WHEN category_path in ("Projectors") then "audio_video"
WHEN category_path in ("Amplifiers and Receivers") then "audio_video"
WHEN category_path in ("Clock Radios") then "audio_video"
WHEN category_path in ("Car speakers, Sub-woofers and Amplifiers") then "automotive"
WHEN category_path in ("Vehicles Lights & Bulbs") then "automotive"
WHEN category_path in ("Vehicle Fluids") then "automotive"
WHEN category_path in ("Car Audio") then "automotive"
WHEN category_path in ("Car Audio and Video Accessories") then "automotive"
WHEN category_path in ("Car Parts") then "automotive"
WHEN category_path in ("Car Care Products") then "automotive"
WHEN category_path in ("Tires & Wheels") then "automotive"
WHEN category_path in ("Motorcycle Parts") then "automotive"
WHEN category_path in ("Car Video") then "automotive"
WHEN category_path in ("Baby Bath & Skin Care") then "baby_product"
WHEN category_path in ("Baby bags") then "baby_product"
WHEN category_path in ("Baby Gears") then "baby_product"
WHEN category_path in ("Baby Toys and Accessories") then "baby_product"
WHEN category_path in ("Baby Safety and Health") then "baby_product"
WHEN category_path in ("Baby Accessories") then "baby_product"
WHEN category_path in ("Diapers") then "baby_product"
WHEN category_path in ("Feeding") then "baby_product"
WHEN category_path in ("Baby Gift Sets") then "baby_product"
WHEN category_path in ("Handbags") then "bags_luggage"
WHEN category_path in ("Apparel, Shoes & Accessories>Bags & Wallets>Wallets") then "bags_luggage"
WHEN category_path in ("Backpacks") then "bags_luggage"
WHEN category_path in ("Trolley Suitcases & Bags") then "bags_luggage"
WHEN category_path in ("Wallets") then "bags_luggage"
WHEN category_path in ("Duffle Bags") then "bags_luggage"
WHEN category_path in ("Specialty Bags & Carry Cases") then "bags_luggage"
WHEN category_path in ("Packing & Bags Accessories") then "bags_luggage"
WHEN category_path in ("Bed Pillows") then "bath_bedding"
WHEN category_path in ("Blankets & Throws") then "bath_bedding"
WHEN category_path in ("Towels") then "bath_bedding"
WHEN category_path in ("Beds, Bed Frames & Accessories") then "bath_bedding"
WHEN category_path in ("Bedding Sets & Components") then "bath_bedding"
WHEN category_path in ("Mattresses") then "bath_bedding"
WHEN category_path in ("Bathroom Accessories") then "bath_bedding"
WHEN category_path in ("Showers & Showerheads") then "bath_bedding"
WHEN category_path in ("Musical Instruments") then "books_media"
WHEN category_path in ("Music Keyboards") then "books_media"
WHEN category_path in ("Education, Learning & Self Help Books") then "books_media"
WHEN category_path in ("Books>Education, Learning & Self Help Books") then "books_media"
WHEN category_path in ("Lifestyle Books") then "books_media"
WHEN category_path in ("Business & Trade Books") then "books_media"
WHEN category_path in ("Movies, Plays and Series") then "books_media"
WHEN category_path in ("Books>Business & Trade Books") then "books_media"
WHEN category_path in ("Music & Movies>Movies, Plays & Series") then "books_media"
WHEN category_path in ("Books>Lifestyle Books") then "books_media"
WHEN category_path in ("maps, atlases & globes") then "books_media"
WHEN category_path in ("Children's Books") then "books_media"
WHEN category_path in ("Books>Children's Books") then "books_media"
WHEN category_path in ("Guitars") then "books_media"
WHEN category_path in ("Interchangeable Lenses") then "camera"
WHEN category_path in ("Electronics>Binoculars & Telescopes") then "camera"
WHEN category_path in ("Security & Surveillance Systems") then "camera"
WHEN category_path in ("Binoculars and Telescopes") then "camera"
WHEN category_path in ("Camcorders") then "camera"
WHEN category_path in ("Digital Cameras") then "camera"
WHEN category_path in ("Analogue Cameras") then "camera"
WHEN category_path in ("Camera and camcorder Accessories") then "camera"
WHEN category_path in ("Beauty Tools and Accessories") then "colour_cosmetics"
WHEN category_path in ("Beauty Gifts and Sets") then "colour_cosmetics"
WHEN category_path in ("Makeup") then "colour_cosmetics"
WHEN category_path in ("Mouse") then "electronic_accessories"
WHEN category_path in ("Cable") then "electronic_accessories"
WHEN category_path in ("Networking Tools") then "electronic_accessories"
WHEN category_path in ("Card Readers / Writers") then "electronic_accessories"
WHEN category_path in ("Hard Drives") then "electronic_accessories"
WHEN category_path in ("Docking Station") then "electronic_accessories"
WHEN category_path in ("Still Films") then "electronic_accessories"
WHEN category_path in ("Power supplies") then "electronic_accessories"
WHEN category_path in ("Electrical & Electronic Accessories") then "electronic_accessories"
WHEN category_path in ("GPS accessories") then "electronic_accessories"
WHEN category_path in ("Power Banks") then "electronic_accessories"
WHEN category_path in ("Computer Monitors") then "electronic_accessories"
WHEN category_path in ("USB Flash Drives") then "electronic_accessories"
WHEN category_path in ("Speakers") then "electronic_accessories"
WHEN category_path in ("Computer Casing") then "electronic_accessories"
WHEN category_path in ("Software") then "electronic_accessories"
WHEN category_path in ("Electronic Flashes") then "electronic_accessories"
WHEN category_path in ("Uninterruptible Power Supply UPS") then "electronic_accessories"
WHEN category_path in ("TV and Satellite Accessories") then "electronic_accessories"
WHEN category_path in ("Network switches") then "electronic_accessories"
WHEN category_path in ("Cooling Pads") then "electronic_accessories"
WHEN category_path in ("Universal Remote Controls") then "electronic_accessories"
WHEN category_path in ("Audio Accessories") then "electronic_accessories"
WHEN category_path in ("Memory Cards") then "electronic_accessories"
WHEN category_path in ("Motherboards") then "electronic_accessories"
WHEN category_path in ("MP3,MP4 Players Accessories") then "electronic_accessories"
WHEN category_path in ("Mobile Phone Accessories") then "electronic_accessories"
WHEN category_path in ("Tablet Accessories") then "electronic_accessories"
WHEN category_path in ("USB Hubs") then "electronic_accessories"
WHEN category_path in ("Network cards & adapters") then "electronic_accessories"
WHEN category_path in ("Memory Modules") then "electronic_accessories"
WHEN category_path in ("Sound Cards") then "electronic_accessories"
WHEN category_path in ("Mobile Phones, Tablets & Accessories>Accessories>Mobile Phone Accessories") then "electronic_accessories"
WHEN category_path in ("Optical Drives") then "electronic_accessories"
WHEN category_path in ("CPUs & Processors") then "electronic_accessories"
WHEN category_path in ("Stereo Systems and Equalizers") then "electronic_accessories"
WHEN category_path in ("Blank DVDs") then "electronic_accessories"
WHEN category_path in ("Headphones & Headsets") then "electronic_accessories"
WHEN category_path in ("Keyboards") then "electronic_accessories"
WHEN category_path in ("Router") then "electronic_accessories"
WHEN category_path in ("Computer & Laptop Accessories") then "electronic_accessories"
WHEN category_path in ("Computers Fans") then "electronic_accessories"
WHEN category_path in ("Webcams") then "electronic_accessories"
WHEN category_path in ("Projectors Accessories") then "electronic_accessories"
WHEN category_path in ("Data Storage devices") then "electronic_accessories"
WHEN category_path in ("Skins & Decals") then "electronic_accessories"
WHEN category_path in ("Batteries") then "electronic_accessories"
WHEN category_path in ("Eyewear Accessories") then "eyewear"
WHEN category_path in ("Contact Lenses") then "eyewear"
WHEN category_path in ("Sandals") then "footwear"
WHEN category_path in ("Athletic Shoes") then "footwear"
WHEN category_path in ("Boots") then "footwear"
WHEN category_path in ("Slippers") then "footwear"
WHEN category_path in ("Shoes Accessories") then "footwear"
WHEN category_path in ("Perfumes & Fragrances") then "fragrance"
WHEN category_path in ("Fragrance Gift Sets") then "fragrance"
WHEN category_path in ("Nursery Furniture & Décor") then "furniture"
WHEN category_path in ("Kitchen & Dining Rooms Furniture Sets") then "furniture"
WHEN category_path in ("Sofas, Bean bags & Ottomans") then "furniture"
WHEN category_path in ("Bedroom Furniture Sets") then "furniture"
WHEN category_path in ("Tables") then "furniture"
WHEN category_path in ("Office Furniture") then "furniture"
WHEN category_path in ("Chairs and benches") then "furniture"
WHEN category_path in ("Air Treatment") then "grocery"
WHEN category_path in ("Seasoning, Spices & Preservatives") then "grocery"
WHEN category_path in ("Air Fresheners") then "grocery"
WHEN category_path in ("Coffee") then "grocery"
WHEN category_path in ("Juices") then "grocery"
WHEN category_path in ("Baby Food") then "grocery"
WHEN category_path in ("Snacks") then "grocery"
WHEN category_path in ("Pet Food & Supplements") then "grocery"
WHEN category_path in ("Pet & Animals Supplies") then "grocery"
WHEN category_path in ("Laundry") then "grocery"
WHEN category_path in ("Pasta & Noodles") then "grocery"
WHEN category_path in ("Dairy Products") then "grocery"
WHEN category_path in ("Pet Food & Supplies>Pet & Animal Supplies") then "grocery"
WHEN category_path in ("Beverages") then "grocery"
WHEN category_path in ("Tea") then "grocery"
WHEN category_path in ("Cleaning Products") then "grocery"
WHEN category_path in ("Sea Food") then "grocery"
WHEN category_path in ("Cereals & Grains") then "grocery"
WHEN category_path in ("Pest Control") then "grocery"
WHEN category_path in ("Confectionery") then "grocery"
WHEN category_path in ("Bakery") then "grocery"
WHEN category_path in ("Ghee") then "grocery"
WHEN category_path in ("Deodorants & Antiperspirants") then "hair_personal_care"
WHEN category_path in ("Shampoos & Conditioners") then "hair_personal_care"
WHEN category_path in ("Hair Care") then "hair_personal_care"
WHEN category_path in ("Hair Extensions Wigs & Accessories") then "hair_personal_care"
WHEN category_path in ("Soap & Shower Gel") then "hair_personal_care"
WHEN category_path in ("Electrical Personal Care") then "hair_personal_care"
WHEN category_path in ("Hair Dyes") then "hair_personal_care"
WHEN category_path in ("Hair Styling Electronics") then "hair_personal_care"
WHEN category_path in ("Oral Care") then "hair_personal_care"
WHEN category_path in ("Hair Tools & Accessories") then "hair_personal_care"
WHEN category_path in ("Men's Grooming") then "hair_personal_care"
WHEN category_path in ("Electric Toothbrushes") then "hair_personal_care"
WHEN category_path in ("Health and Personal Care") then "hair_personal_care"
WHEN category_path in ("Skin Care") then "hair_personal_care"
WHEN category_path in ("Bath & Body") then "hair_personal_care"
WHEN category_path in ("Electric Shavers & Removal") then "hair_personal_care"
WHEN category_path in ("Beauty>Wigs") then "hair_personal_care"
WHEN category_path in ("Dietary Supplements") then "health_nutrition"
WHEN category_path in ("Body Massagers") then "health_nutrition"
WHEN category_path in ("Health & Personal Care>Small Medical Equipment") then "health_nutrition"
WHEN category_path in ("Small Medical Equipment") then "health_nutrition"
WHEN category_path in ("Sports Nutrition") then "health_nutrition"
WHEN category_path in ("Vitamins & Minerals") then "health_nutrition"
WHEN category_path in ("Juicers & Presses") then "home_appliances"
WHEN category_path in ("Range Hoods") then "home_appliances"
WHEN category_path in ("Irons") then "home_appliances"
WHEN category_path in ("Sandwich & Waffle Makers") then "home_appliances"
WHEN category_path in ("Dishwashers") then "home_appliances"
WHEN category_path in ("Rice Cooker") then "home_appliances"
WHEN category_path in ("Barbecue Grills & Smokers") then "home_appliances"
WHEN category_path in ("Microwaves") then "home_appliances"
WHEN category_path in ("Vacuum Cleaners") then "home_appliances"
WHEN category_path in ("Blenders & Mixers") then "home_appliances"
WHEN category_path in ("Water Coolers & Dispensers") then "home_appliances"
WHEN category_path in ("Specialty Kitchen Appliances") then "home_appliances"
WHEN category_path in ("Fans") then "home_appliances"
WHEN category_path in ("Refrigerators & Freezers") then "home_appliances"
WHEN category_path in ("Washing Machines") then "home_appliances"
WHEN category_path in ("Dryers") then "home_appliances"
WHEN category_path in ("Washers Dryers 2 in 1") then "home_appliances"
WHEN category_path in ("Water Heaters") then "home_appliances"
WHEN category_path in ("Kettles") then "home_appliances"
WHEN category_path in ("Food Processors") then "home_appliances"
WHEN category_path in ("Appliances Parts & Accessories") then "home_appliances"
WHEN category_path in ("Sewing and Accessories") then "home_appliances"
WHEN category_path in ("Food Preparation") then "home_appliances"
WHEN category_path in ("Ovens, Ranges & Stoves") then "home_appliances"
WHEN category_path in ("Fryers") then "home_appliances"
WHEN category_path in ("Electric Slicers") then "home_appliances"
WHEN category_path in ("Steam Cleaners") then "home_appliances"
WHEN category_path in ("Heaters") then "home_appliances"
WHEN category_path in ("Coffee & Espresso Makers") then "home_appliances"
WHEN category_path in ("Electric Meat Grinders") then "home_appliances"
WHEN category_path in ("Toasters") then "home_appliances"
WHEN category_path in ("Vacuums & Floor Care Accessories") then "home_appliances"
WHEN category_path in ("Curtains & Accessories") then "home_decor"
WHEN category_path in ("Smoking Accessories") then "home_decor"
WHEN category_path in ("Digital Photo Frames") then "home_decor"
WHEN category_path in ("Wallpaper & Decals") then "home_decor"
WHEN category_path in ("Light Bulbs") then "home_decor"
WHEN category_path in ("Flowers") then "home_decor"
WHEN category_path in ("Lamps & Lightings") then "home_decor"
WHEN category_path in ("Islamic, Ethnic and Digital Art") then "home_decor"
WHEN category_path in ("Clocks") then "home_decor"
WHEN category_path in ("Rugs & Carpets") then "home_decor"
WHEN category_path in ("Garden decoration") then "home_decor"
WHEN category_path in ("Frames") then "home_decor"
WHEN category_path in ("Mirrors") then "home_decor"
WHEN category_path in ("Home Decor & Furniture>Home Decor Center>Clocks") then "home_decor"
WHEN category_path in ("Glasses Frames") then "home_decor"
WHEN category_path in ("Decorative Pillows & Cushions") then "home_decor"
WHEN category_path in ("Candles") then "home_decor"
WHEN category_path in ("Home Decor & Furniture>Lamps & Lighting>Lighting & Lamps") then "home_decor"
WHEN category_path in ("Gardening and Watering equipments") then "home_improvement"
WHEN category_path in ("Tools & Home Improvements>Hand Tools") then "home_improvement"
WHEN category_path in ("Home Supplies") then "home_improvement"
WHEN category_path in ("Paint & Supplies") then "home_improvement"
WHEN category_path in ("Hand tools") then "home_improvement"
WHEN category_path in ("Ironing Accessories") then "home_improvement"
WHEN category_path in ("Power Tools") then "home_improvement"
WHEN category_path in ("Measuring & Layout Tools") then "home_improvement"
WHEN category_path in ("Power & Hand Tools Accessories") then "home_improvement"
WHEN category_path in ("Nails, Screws & Fixings") then "home_improvement"
WHEN category_path in ("Work Safety Equipment & Gear") then "home_improvement"
WHEN category_path in ("Flashlights") then "home_improvement"
WHEN category_path in ("Earrings") then "jewelry"
WHEN category_path in ("Men's Jewelry") then "jewelry"
WHEN category_path in ("Pendants & Charms") then "jewelry"
WHEN category_path in ("loose gemstones & diamonds") then "jewelry"
WHEN category_path in ("Jewelry Accessories") then "jewelry"
WHEN category_path in ("Fine Jewellery") then "jewelry"
WHEN category_path in ("Drinkware") then "kitchen_dining"
WHEN category_path in ("Kitchen Accessories") then "kitchen_dining"
WHEN category_path in ("Barbecue Tools and Grill Accessories") then "kitchen_dining"
WHEN category_path in ("Cookware") then "kitchen_dining"
WHEN category_path in ("Kitchen Measuring Tools") then "kitchen_dining"
WHEN category_path in ("Plastic & Paper Products") then "kitchen_dining"
WHEN category_path in ("Bakeware & Accessories") then "kitchen_dining"
WHEN category_path in ("Cooking Utensils") then "kitchen_dining"
WHEN category_path in ("Table Linens") then "kitchen_dining"
WHEN category_path in ("Kitchen & Home Supplies>Cutlery & Flatware Set") then "kitchen_dining"
WHEN category_path in ("Cutlery & Flatware Sets") then "kitchen_dining"
WHEN category_path in ("Storage & Organization") then "kitchen_dining"
WHEN category_path in ("Kitchen Scales") then "kitchen_dining"
WHEN category_path in ("Dinnerware & Serveware") then "kitchen_dining"
WHEN category_path in ("PCs") then "laptops"
WHEN category_path in ("Mobile Phones") then "mobiles"
WHEN category_path in ("E-book Readers") then "mobiles"
WHEN category_path in ("Tablets") then "mobiles"
WHEN category_path in ("GPS Navigators") then "sports_outdoor"
WHEN category_path in ("GPS Receiver") then "sports_outdoor"
WHEN category_path in ("Treadmills") then "sports_outdoor"
WHEN category_path in ("Bikes & Bicycles") then "sports_outdoor"
WHEN category_path in ("Sports Equipments") then "sports_outdoor"
WHEN category_path in ("Camping Goods") then "sports_outdoor"
WHEN category_path in ("Rackets") then "sports_outdoor"
WHEN category_path in ("Sport Gloves") then "sports_outdoor"
WHEN category_path in ("Scooters & Ride-Ons") then "sports_outdoor"
WHEN category_path in ("Weights & Dumbbells") then "sports_outdoor"
WHEN category_path in ("Protective Gear") then "sports_outdoor"
WHEN category_path in ("Exercise bikes") then "sports_outdoor"
WHEN category_path in ("3D Printers") then "stationery_office_supplies"
WHEN category_path in ("Ink Cartridges") then "stationery_office_supplies"
WHEN category_path in ("Barcode reader") then "stationery_office_supplies"
WHEN category_path in ("Fax Machines and Shredders") then "stationery_office_supplies"
WHEN category_path in ("Stationery") then "stationery_office_supplies"
WHEN category_path in ("Office Supplies") then "stationery_office_supplies"
WHEN category_path in ("Telephone Accessories") then "stationery_office_supplies"
WHEN category_path in ("Printers") then "stationery_office_supplies"
WHEN category_path in ("Telephones") then "stationery_office_supplies"
WHEN category_path in ("Printer and Scanner Accessories") then "stationery_office_supplies"
WHEN category_path in ("Stamps") then "stationery_office_supplies"
WHEN category_path in ("Office equipment") then "stationery_office_supplies"
WHEN category_path in ("Scanners") then "stationery_office_supplies"
WHEN category_path in ("Multifunctional Printers") then "stationery_office_supplies"
WHEN category_path in ("Label Printers") then "stationery_office_supplies"
WHEN category_path in ("Party Supplies") then "toys"
WHEN category_path in ("Outdoor Play") then "toys"
WHEN category_path in ("Costumes & Accessories") then "toys"
WHEN category_path in ("Sports & Fitness>Outdoor Play") then "toys"
WHEN category_path in ("Paper Money") then "toys"
WHEN category_path in ("Games Gadgets & Accessories") then "video_games"
WHEN category_path in ("Video Games") then "video_games"
WHEN category_path in ("Game Consoles") then "video_games"
WHEN category_path in ("Watches") then "watches"
WHEN category_path in ("VR Gadgets") then "wearables"
WHEN category_path in ("Smart Watches & Watches Accessories") then "wearables"
WHEN category_path in ("Smart Watches") then "wearables"
WHEN category_path in ("3D Glasses") then "wearables"
WHEN category_path in ("Fitness Technology") then "wearables"
WHEN category_path in ("Screen Protectors") then "electronic_accessories"
WHEN category_path in ("Chargers") then "electronic_accessories"
WHEN category_path in ("Balls") then "sports_outdoor"
WHEN category_path in ("Toys") then "toys"
WHEN category_path in ("Casual & Dress Shoes") then "footwear"
WHEN category_path in ("Books>Literature & Fiction") then "books_media"
WHEN category_path in ("Necklaces") then "jewelry"
WHEN category_path in ("Literature & Fiction") then "books_media"
WHEN category_path in ("Baby Clothes & Shoes") then "baby_product"
WHEN category_path in ("Sporting Goods") then "sports_oudoor"
WHEN category_path in ("Jewelry sets") then "jewelry"
WHEN category_path in ("Bracelets") then "jewelry"
WHEN category_path in ("Eyewear") then "eyewear"
WHEN category_path in ("Home Decor") then "home_decor"
WHEN category_path in ("Coins, Stamps & Paper money>Paper Money") then "home_decor"
WHEN category_path in ("Coins, Stamps & Paper money>Stamps") then "home_decor"
WHEN category_path in ("Art, Crafts & Collectables>Prints & Posters") then "home_decor"
WHEN category_path in ("Keys & key chains") then "automotive"
WHEN category_path in ("Laptops & Notebooks") then "laptops"
WHEN category_path in ("Prints, Posters & Photographs") then "home_decor"
WHEN category_path in ("Musical Instruments Parts") then "books_media"
WHEN category_path in ("Drills") then "home_improvement"
WHEN category_path in ("Rings") then "jewelry"
WHEN category_path in ("handcrafts, sculpture & carvings") then "home_decor"
WHEN category_path in ("Drawings & Paintings") then "home_decor"
WHEN category_path in ("Personal Scales") then "bath_bedding"
WHEN category_path in ("Mobile Phone Chargers") then "electronic_accessories"
WHEN category_path in ("Art, Crafts & Collectables>Islamic") then "home_decor"
WHEN category_path in ("Air Conditioners & Coolers") then "home_appliances"
WHEN category_path in ("Music CDs") then "books_media"
WHEN category_path in ("Recording and Studio Equipment") then "books_media"
WHEN category_path in ("Fruits & Vegetables") then "grocery"
WHEN category_path in ("Video Cards") then "video_games"
WHEN category_path in ("Art, Crafts & Collectables>Antiques") then "home_decor"
WHEN category_path in ("Garden Furniture") then "furniture"
WHEN category_path in ("Coins, Stamps & Paper money>Coins") then "home_decor"
WHEN category_path in ("Antiques") then "home_decor"
WHEN category_path in ("Comics & Graphic Novels") then "books_media"
WHEN category_path in ("Living Room sets") then "furniture"
WHEN category_path in ("Coins & Stamps Accessories") then "home_decor"
WHEN category_path in ("Jewelry & Accessories>Bracelets") then "jewelry"
WHEN category_path in ("Server") then "electronic_accessories"
WHEN category_path in ("Art, Crafts & Collectables>Handcrafts, Sculpture & Carvings") then "home_decor"
WHEN category_path in ("Coins") then "home_decor"
WHEN category_path in ("Jewelry & Accessories>Rings") then "jewelry"
WHEN category_path in ("Music & Movies>Music CDs") then "books_media"
WHEN category_path in ("Books>Comics & Graphic Novels") then "books_media"
WHEN category_path in ("Thermometers") then "health_nutrition"
WHEN category_path in ("Art, Crafts & Collectables>Drawings & Paintings") then "home_decor"
WHEN category_path in ("Garden & Outdoor>Smoking Accessories") then "home_decor"
WHEN category_path in ("Health & Personal Care>Personal Scales") then "bath_bedding"
WHEN category_path in ("Kitchen & Home Supplies>Home Supplies") then "kitchen_dining"
WHEN category_path in ("Sports & Fitness>Sporting Goods") then "sports_outdoor"
WHEN category_path in ("Jewelry & Accessories>Necklaces") then "jewelry"
WHEN category_path in ("Tuners") then "books_media"
WHEN category_path in ("Apparel, Shoes & Accessories>Clothing>Accessories>Scarves & Wraps") then "apparel"
WHEN category_path in ("Cameras>Screen Protectors") then "electronic_accessories"
WHEN category_path in ("Blank CDs") then "audio_video"
WHEN category_path in ("CD Recording Media") then "audio_video"
WHEN category_path in ("Health & Personal Care>Digital Fever Thermometers") then "health_nutrition"
WHEN category_path in ("Home Appliances>Heating, Cooling & Air Quality Center>Heaters") then "home_appliances"
WHEN category_path in ("Home Appliances>Heating, Cooling & Air Quality Center>Air Treatment") then "home_appliances"
WHEN category_path in ("Toys>Toys") then "toys"
WHEN category_path in ("Jewelry & Accessories>Jewelry Sets") then "jewelry"
WHEN category_path in ("Home Decor & Furniture>Home Decor Center>Rugs & Carpets") then "home_decor"
WHEN category_path in ("Computers, IT & Networking>Networking & Accessories>Blank CDs") then "audio_video"
WHEN category_path in ("Apparel, Shoes & Accessories>Clothing>Tops, T-Shirts, Polos & Shirts") then "apparel"
WHEN category_path in ("Garden & Outdoor>Barbecue Tools & Grill Accessories") then "kitchen_dining"
WHEN category_path in ("Vehicle Parts & Accessories>Accessories>Keys & Key Chains") then "automotive"
WHEN category_path in ("Garden & Outdoor>Pest Control") then "home_improvement"
WHEN category_path in ("Baby>Baby Bags") then "baby_product"

  END as reporting_category_parent
from
`noonbicedata.catalog_v2.assortment_monthly`
where
website_name in ("SOUQ_EG")
) ""
